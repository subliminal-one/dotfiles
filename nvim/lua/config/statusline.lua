local lualine = require('lualine');

lualine.setup({
  options = {
    theme = "auto",
    icons_enabled = false,
    section_separators = '',
    component_separators = '',
    path = 1,
    globalstatus = true,
  },
});

local lualine = require('lualine');

lualine.setup({
  options = {
    theme = "onenord",
    icons_enabled = false,
    section_separators = '',
    component_separators = '',
    path = 1,
  },
});
